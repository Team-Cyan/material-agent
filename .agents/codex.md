# Codex Entry

Follow `AGENTS.md` and its task routing. Do not reload instructions already in context. Repository guidance is portable and does not require machine-global files or a particular coding model.

- General working rules and verification: `docs/ai/shared-context.md`.
- Local evaluation: `docs/ai/modules/local-benchmark.md`.
- Explicit OMLX comparison work: `.agents/harness-engineering.md`.
- Optional task prompts: `docs/ai/prompts/debug.md` and `docs/ai/prompts/feature.md`.
- Bounded delegation, when useful and supported by the active runtime: `docs/ai/templates/subagent-task.md`.

Continue the authorized objective across turns and compaction. Keep model and reasoning selection in runtime configuration; application inference choices belong in `docs/ai/model-selection.md`.
